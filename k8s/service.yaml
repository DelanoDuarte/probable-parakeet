apiVersion: v1
kind: Service
metadata:
  name: appointment-scheduling
  namespace: scheduling
  labels:
    app: appointment-scheduling
spec:
  type: ClusterIP
  selector:
    app: appointment-scheduling
  ports:
    - name: http
      port: 80
      targetPort: http
